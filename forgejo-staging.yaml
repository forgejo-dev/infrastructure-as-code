---
- name: Check SSH port
  import_playbook: ssh.yaml
- name: Deploy forgejo
  hosts: staging
  roles:
    - reverse-proxy
    - forgejo-dc
    # Woodpecker was replaced by Forgejo Actions.
    - woodpecker-dc
    - minio-dc
  become: true
  vars:
    instance_url: "{{staging_url}}"
    woodpecker_mode: "woodpecker_staging.yml"
